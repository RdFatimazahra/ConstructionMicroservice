CREATE TABLE utilisateur (
                             idUser SERIAL PRIMARY KEY,
                             nom VARCHAR(50) ,
                             email VARCHAR(50),
                             password VARCHAR(50)
);